# ADR 0003: Evidence-based provenance

Status: Accepted.

Treat filesystem, filename, container, sidecar, importer, and user metadata as independent evidence with origin and confidence. Unknown and conflicting fields remain valid. No vendor naming contract controls ingestion. User evidence should receive highest confidence once correction UI is added.

